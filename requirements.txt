# Core detection & tracking
ultralytics>=8.2.0          # YOLOv8 + ByteTrack (bundled)
opencv-python>=4.9.0        # Video I/O and drawing primitives

# UI
streamlit>=1.34.0

# Numerics (pinned floors; ultralytics will pull in compatible versions)
numpy>=1.24.0

# Image display in Streamlit
Pillow>=10.0.0

# Optional — GPU acceleration (uncomment if you have CUDA)
# torch>=2.0.0+cu118
# torchvision>=0.15.0+cu118
